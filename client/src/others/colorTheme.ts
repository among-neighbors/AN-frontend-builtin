import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      // light: '#ff9d3f',
      main: '#000',
      // dark: '#b53d00',
      contrastText: '#EC8034',
    },
    secondary: {
 
      main: '#000',
      contrastText: '#fff',
     
    },
    
  },
});

export default theme;
